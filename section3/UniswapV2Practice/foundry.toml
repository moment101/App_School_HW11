[profile.default]
src = "src"
out = "out"
libs = ["lib"]

[rpc_endpoints]
mainnet = "${RPC_URL}"

# rpc_endpoints = { mainnet = "" }
# See more config options https://github.com/foundry-rs/foundry/tree/master/config
